<% flash.each do |type, msg| %>
  <div class="ui <%= type %> message transition">
    <i class="close icon"></i>
    <div class="header">
      <%= type.titleize %>
    </div>
    <p><%= msg %></p>
  </div>
<% end %>